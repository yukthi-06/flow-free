# User Prompt
```
remove achievement banners
```

# Achievement Banner Removal Log

This is a minor UI update to remove the pop-up achievement banners from the game screen. 

## Changes Made
- Removed the `AchievementNotification` composable block from `MainActivity.kt` so that achievements continue to track silently in the background without interrupting the UI with banners.
