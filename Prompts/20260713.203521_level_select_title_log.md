# User Prompt
```
when user clicks on 5x5, it takes us to a level selector screen.  This screen's current title is 'Level Select'.  It should be '5x5 Level Select'
```

# Level Select Title Update Log

This is a minor UI text change to make the level selector screen title dynamically indicate which pack you are currently browsing.

## Changes Made
- Updated the `TopAppBar` title inside `LevelScreen` within `MainActivity.kt`. It now dynamically prepends `${pack.name}` to the existing `R.string.level_selector` string resource.
