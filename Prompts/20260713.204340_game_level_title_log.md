# User Prompt
```
individual game level should show the packname  in title.  as of now, i can only know the level number.
```

# Game Level Title Update Log

This is a minor UI text change to ensure the individual game screen displays which pack the user is currently playing.

## Changes Made
- Updated the `TopAppBar` inside the `GameScreen` composable (`MainActivity.kt`) to render a `Text(pack.name)` widget as its title instead of passing an empty `{}`.
